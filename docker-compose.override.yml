services:
  bp_sync:
    volumes:
      - ./bp_sync/src:/app/src
    env_file:
      - ./.env
    ports:
     - 8000:8000

  seq:
    volumes:
      - seq-data:/data
    env_file:
      - ./.env
    ports:
      - "5341:80"

volumes:
  seq-data:
